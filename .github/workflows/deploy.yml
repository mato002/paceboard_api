# .github/workflows/deploy.yml
# GitHub Actions workflow to automatically deploy website files to a production server via FTP on every push to the main branch.

name: Production Deployment via FTP

on:
  push:
    branches:
      - main  # Change this to 'master' if your default branch uses the old naming convention

jobs:
  deploy:
    name: FTP Deploy Job
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout the repository code so the workflow can access its files
      - name: Checkout Repository Code
        uses: actions/checkout@v4

      # Laravel application (app, routes, vendor stays on server). Not the public folder.
      - name: Sync Laravel App To Server
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          server-dir: /paceboard/
          exclude: |
            **/.git*
            **/.github/**
            **/node_modules/**
            **/vendor/**
            **/storage/logs/**
            **/storage/framework/cache/**
            **/storage/framework/sessions/**
            **/storage/framework/views/**
            **/.env
            **/.env.*

      # Document root from File Manager: /public_html/paceboard
      # Only add deploy-hook.php; never replace index.php.
      - name: Upload Hook To Public Folder
        env:
          FTP_SERVER: ${{ secrets.FTP_SERVER }}
          FTP_USERNAME: ${{ secrets.FTP_USERNAME }}
          FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
        run: python3 deploy-web/upload_hook.py

      - name: Run Post-Deploy Hook
        env:
          DEPLOY_HOOK_TOKEN: ${{ secrets.DEPLOY_HOOK_TOKEN }}
        run: |
          if [ -z "$DEPLOY_HOOK_TOKEN" ]; then
            echo "DEPLOY_HOOK_TOKEN secret is missing."
            exit 1
          fi

          GET_CODE="$(curl -sS -o /tmp/hook_get -w '%{http_code}' 'https://paceboard.zhengkyc.com/deploy-hook.php')"
          echo "GET https://paceboard.zhengkyc.com/deploy-hook.php => HTTP $GET_CODE"
          cat /tmp/hook_get || true
          echo
          if [ "$GET_CODE" != "405" ]; then
            echo "deploy-hook.php is still not in /public_html/paceboard."
            exit 1
          fi

          HTTP_CODE="$(curl -sS -o /tmp/hook_resp -w '%{http_code}' \
            -X POST 'https://paceboard.zhengkyc.com/deploy-hook.php' \
            -H "X-Deploy-Token: $DEPLOY_HOOK_TOKEN" \
            -H 'Accept: application/json' \
            -H 'Content-Type: application/json')"
          echo "POST deploy-hook.php => HTTP $HTTP_CODE"
          cat /tmp/hook_resp || true
          echo
          if [ "$HTTP_CODE" = "401" ] || [ "$HTTP_CODE" = "403" ]; then
            echo "Hook file is live. Set in /paceboard/.env: DEPLOY_HOOK_ENABLED=true and DEPLOY_HOOK_TOKEN matching GitHub."
            exit 1
          fi
          if [ "$HTTP_CODE" != "200" ]; then
            exit 1
          fi
